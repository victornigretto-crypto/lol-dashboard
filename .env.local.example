NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
RIOT_API_KEY=RGAPI-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Cle service_role de Supabase (Dashboard > Project Settings > API > service_role).
# Sert UNIQUEMENT au cache partage des parties (table match_facts), qui n'a pas
# de proprietaire : un visiteur anonyme n'a pas d'auth.uid(), donc aucune policy
# RLS ne peut l'autoriser a ecrire.
#
# Elle contourne RLS : ne jamais la prefixer NEXT_PUBLIC_, ne jamais l'importer
# dans un composant client. Sans elle l'app fonctionne, mais sans cache : chaque
# recherche retelecharge les 20 parties depuis l'API Riot.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# Cle API Resend (https://resend.com > API Keys), pour le bouton
# "Rapporter un probleme". Appelee en HTTPS depuis app/api/feedback/route.ts :
# aucun paquet npm n'est installe pour ca.
#
# Sans elle, le bouton et sa fenetre s'affichent et se remplissent normalement,
# mais l'envoi echoue : l'utilisateur voit un message generique, et la cause
# exacte part dans les logs serveur.
#
# ATTENTION tant qu'aucun domaine n'est verifie chez Resend : l'expediteur doit
# rester onboarding@resend.dev, et Resend n'accepte d'envoyer QU'A l'adresse
# proprietaire du compte. Creer le compte Resend avec grosgalio@gmail.com, ou
# verifier un domaine.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
